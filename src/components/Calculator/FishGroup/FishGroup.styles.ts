import { FishGroupStyles } from './FishGroup.types'

const makeStyles = (): FishGroupStyles => ({
	fishAmountInput: {
		border: '1px solid #ccc',
        borderRadius: '5px',
        fontSize: '16px',
        padding: '10px',
		width: '50px'
	}
})

export default makeStyles
