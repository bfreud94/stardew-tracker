import { NoteInputStyles } from './NoteInput.types'

const makeStyles = (): NoteInputStyles => ({
	noteInput: {
		height: '200px',
		marginTop: '20px',
		width: '100%'
	}
})

export default makeStyles
